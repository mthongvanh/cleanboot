/// Parameters for fetching the application's display names
class GetDisplayNamesParams {

  /// Cache type when fetching data locally or remotely
  final String? cacheType;

  /// Parameters for fetching the application's display names
  GetDisplayNamesParams({this.cacheType});
}
